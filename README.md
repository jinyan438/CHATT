# CHATT

## 状态栏渲染

现在聊天页面会自动识别并渲染内嵌在消息里的 `<maintext>` 与 `<Status_block>` 结构：

1. 当 AI 回复中出现合法的 YAML 状态块时，消息气泡会直接显示地点、时间、剧情正文、角色状态以及行动选项。
2. 状态栏支持中文/英文字段名混用，所有角色属性与行动列表都会自动格式化为卡片。
3. 如果 YAML 解析失败或字段缺失，气泡中会出现错误提示，方便第一时间定位“加载中”类问题。

无需再额外复制到示例文件中调试，直接在主界面即可查看完整的状态栏渲染效果。
